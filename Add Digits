class Solution:
    def addDigits(self, num: int) -> int:
        while num >= 10:
            digits_sum = 0
            while num > 0:
                digits_sum += num % 10
                num //= 10
            num = digits_sum
        return num
